buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.9.1'
        classpath 'com.google.gms:google-services:4.3.15' // Optional, only if you're using Firebase
    }
}

plugins {
    // Remove or comment this out if you're using Groovy DSL instead of version catalogs
    // alias(libs.plugins.android.application) apply false
}
